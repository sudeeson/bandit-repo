# import socket

# def insecure_bind():
#     s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
#     s.bind(('0.0.0.0', 8080))  # Binding to all interfaces, allowing external access
#     s.listen(5)